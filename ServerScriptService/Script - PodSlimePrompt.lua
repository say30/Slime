-- from: ServerScriptService.PodSlimePrompt

-- ServerScriptService/PodSlimePrompt.lua - Gère les pods vides (sans ProximityPrompt)
-- Place: ServerScriptService
-- Type: Script

local Workspace = game:GetService("Workspace")
local RS = game:GetService("ReplicatedStorage")

print("🔍 PodSlimePrompt: Démarrage...")

-- Ce script ne fait plus rien car les ProximityPrompt sont maintenant sur les slimes
-- On garde le script pour compatibilité mais il ne crée plus de prompts sur les pods

print("✅ PodSlimePrompt terminé - Les prompts sont maintenant sur les slimes")
